extends layout
block content
  h3 User Details
  p Name: #{user.name}
  h3 Tweets
  if user.tweets.length
    ol
      each tweet in user.tweets
        if tweet.originalTweet
          li "#{tweet.content}" with the original tweet "#{tweet.originalTweet.content} (#{tweet.id})"
        else 
          li "#{tweet.content} (#{tweet.id})"
  else
    p #{user.name} has no tweet yet!
  